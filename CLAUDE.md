# Память проекта

## Сборка и dev-сервер дерутся за .next

`npm run build` и запущенный `next dev` используют одну папку `.next`:
сборка при живом dev-сервере падает загадочным `TypeError: a[d] is not a
function` в webpack-runtime, а dev после сборки сыплет ENOENT на чанки.
Перед `npm run build` останавливать dev-сервер; после — перезапускать.

## Порт 3000 бывает занят чужим проектом

На этой машине на :3000 может висеть Remotion Studio из `reels-subtitles`.
Dev-сервер поднимать через `.claude/launch.json` (`belajar-bahasa`, autoPort) —
он сам уходит на свободный порт. Файл launch.json уже существует — не
перезаписывать, а править.

## Voicer: квота списывается на создании задачи, а не на озвучке

`Квота: …` в скрипте — это **остаток**, а не расход (раньше строка читалась как
«истрачено X из 500 000» и вводила в заблуждение; поправлено). Символы уходят
в момент `POST /voice/synthesize`, ещё до того, как задача что-то озвучила.
Задача, повисшая на 0%, квоту держит и сама её не отпускает — вернуть можно
только `POST /api/v1/voice/cancel/{task_id}`, а списка задач у API нет вовсе.
Отсюда правило: **id каждой созданной задачи писать в журнал сразу**
(`data/audio-tasks.jsonl`), иначе зависшую задачу нечем ни докачать, ни отменить.

Прежняя запись в этой памяти утверждала, что перезапуск скрипта «доберёт
недостающее без повторной траты квоты». Это неверно: каждый прогон создаёт
новые задачи и списывает символы заново.

## Voicer ломается на стороне сервиса, а не голоса

Симптом: все задачи стоят на 0%, файлов нет, скрипт висит вечно. Смотреть не в
логи скрипта, а в `GET /api/v1/voice/status/{task_id}` — там лежит настоящая
причина. В августе 2026 это было `status: pending_confirmation` и
«конвейер hCaptcha-токенов встал», то есть у Voicer сломан путь к ElevenLabs,
и он предлагает failover на HeyGen — **другого провайдера, то есть другой
голос**. Соглашаться на такой failover молча нельзя: у книги один рассказчик,
и половина глав чужим голосом хуже, чем отсутствие озвучки. Это решение
владельца.

В `waitTask` теперь есть стоп-таймеры (5 мин без движения прогресса, 20 мин на
задачу) — раньше их не было и скрипт зависал навсегда, пряча ошибку.
## Аналитика читалки

События поведения читателей: `lib/track.ts` (клиентская очередь + beacon) →
`app/api/track/route.ts` (service-role insert) → таблица `reading_events` и
витрины (`supabase/migrations/00004_reading_events.sql`) → админка `/admin`.
Работает только при заданных `SUPABASE_SERVICE_ROLE_KEY` и `ADMIN_EMAILS`
в окружении сервера; без них события молча отбрасываются, `/admin` — 404.
user_id клиенту не доверяем — сервер берёт его из cookies-сессии, анонимная
история склеивается с аккаунтом по `anon_id` при первом залогиненном батче.

## Иллюстрации книг: подписка Google AI Pro ≠ квота API

Подписка Google AI Pro даёт картинки только в приложении Gemini и в веб-UI
AI Studio. На API-ключ она не распространяется вообще: у image-моделей
(`gemini-3.1-flash-image`, `gemini-2.5-flash-image`) free tier равен нулю, и
ключ без биллинга отвечает 429 `free_tier_requests, limit: 0`. В самом
Playground AI Studio Nano Banana 2 тоже просит платный ключ. Значит
`scripts/generate-illustrations.mjs` заработает только после включения
биллинга в Google Cloud — это делает владелец аккаунта, карту вводить не мне.

Выкачать картинку из браузера в обход этого нельзя: в песочнице заблокированы
и скачивание, и запросы к 127.0.0.1 (private network), и буфер обмена, а
картинки Gemini лежат за авторизацией на `lh3.googleusercontent.com`.

## Переписал книгу — потерял озвучку

Ключ и перевода, и MP3 — сам текст предложения (`translations.json` по тексту,
имя файла озвучки по его FNV-1a хэшу). Пока предложение не менялось, ключ тот же,
и перепарсивание книги ничего не ломает. Но переписанная книга — это новые
предложения: перевод доберётся `translate-reading.mjs` по подписке бесплатно,
а озвучка отвалится целиком и стоит квоты Voicer. Считать это в цену задачи
заранее и спрашивать владельца, а не жечь квоту молча.

## Объём книги: страницы = слова / 250

В проекте «страница» — 250 английских слов (так посчитан объём «Основателей»
в их библии). Просьбу «сделай 100–150 страниц» переводить в 25 000–37 500 слов
EN и мерить `node scripts/check-level.mjs <slug>` — он же считает, сколько
РАЗНЫХ слов надо знать (леммы без имён собственных) и показывает редкие слова,
не объяснённые во врезке «New Words». Для A2 ориентир — 1000–2000 лемм.

## Служба на студии жила вне ~/Code — и push до неё не доезжал

До 27 августа 2026 launchd-агент `com.almir.belajar-bahasa` был поднят из
`~/belajar-bahasa` — копии на гигабайт, которая вообще не была git-репозиторием
(ни `origin`, ни истории). Книга уезжала на GitHub и на Vercel, `~/Code` на
студии обновлялся, а читатель на `http://mac-studio:8766` месяцами открывал
старую сборку, и никакая синхронизация этого не чинила.

Агент переведён на `~/Code/belajar-bahasa` (там же `.env.local` и
`node_modules`), старая копия не удалена — лежит на всякий случай.

Вывод на будущее: «выложено» — это не про файлы, а про то, откуда стартует
процесс. Проверять `lsof -a -d cwd -p $(lsof -ti :<порт>)`, а не наличие
свежего коммита в папке. И страницы книг статические: после обновления текста
нужен `npm run build` + перезапуск агента, иначе на диске новое, а в браузере
старое.

## Сказки — отдельный раздел, не книга читалки

`/skazki` намеренно не использует движок читалки. У читалки другая задача:
индонезийский текст, перевод по тапу, глоссарий, озвучка каждого слова, учёт
прогресса. Русской сказке, которую взрослый читает вслух четырёхлетке, не нужно
ничего из этого — нужен крупный текст и картинки. Поэтому у раздела свои данные
(`data/skazki/`), своя библиотека (`lib/skazki.ts`) и свои страницы.

Раздел **не закрыт логином**: в `lib/supabase/middleware.ts` под аккаунт уходят
только `/lessons` и `/reading`. Это решение, а не недосмотр — сказку читают
бабушка, няня, второй родитель, и заводить им аккаунт незачем.

Конвейер (всё идемпотентно, прерванный прогон догоняется перезапуском):

```bash
npm run skazki:build    # части parts/p01..p20.json → книга + промпты
npm run skazki:images   # 201 картинка локально через mflux, ~85 с на штуку
npm run skazki:webp     # PNG → WebP, иначе картинки весят 400 МБ
./scripts/run-skazki.sh # всё вместе, с ожиданием дописываемых частей
```

Генератор `generate-illustrations-local.py` теперь общий для обоих разделов:
пути задаются флагами `--prompts` и `--out-dir`, по умолчанию — как было у книг.

**PNG обязательно переводить в WebP.** У книг читалки рядом лежат обе версии, и
папка одной книги занимает 88 МБ. Здесь картинок вчетверо больше: 200 PNG — это
около 400 МБ в git и в деплое, в WebP те же 200 — около 30 МБ.

**id картинки = номер части × 100 + номер картинки.** Сид генератора считается
от id, поэтому повторившийся id молча даст две одинаковые картинки. Сборщик
`build-skazki.mjs` проверяет уникальность и падает, если id пересеклись.

**Книгу целиком рисовать на аренде, но той же моделью.** Локально 221 картинка
считается пять часов, на арендованной A6000 той же Z-Image — час и полдоллара
(`./scripts/gen-skazki-gpu.sh ezhinka-ulya zimage`, дальше `destroy`). Менять
модель посреди книги нельзя: у FLUX.2 и Z-Image разный рисунок, и это видно на
каждом развороте. Если уж менять — перерисовывать всё, а старое убирать, иначе
генератор пропустит готовые файлы и книга останется смешанной.

**Обложки — два слоя, и это не украшательство.** Модель рисует арт в
`data/skazki/<slug>/art/` (папка в .gitignore), название кладёт поверх
`make-skazki-covers.py`. Если бы текст рисовала модель, вместо букв была бы
каша, а перерисовка арта затирала бы готовую обложку. Промпты обложек написаны
с пустым верхом: там ложится кремовая плашка с названием.

## Перехват звука: элемент встаёт молча, без единого события

В машине озвучка «проигрывала пару карточек и вставала на паузу», если рядом
работал навигатор. Причина не в сети и не в файлах: когда чужое приложение
(подсказка Google Maps, звонок) забирает аудиосессию, iOS просто останавливает
`<audio>` — ни `ended`, ни `error` не приходит. Вся очередь озвучки построена
на этих событиях, поэтому обещание в `playUrl` повисало навсегда и петля
замирала до перезагрузки страницы.

Правило: **любое ожидание медиа-события должно иметь сторожа**. В
`lib/audio-element.ts` он есть: пока звук должен идти, `timeupdate` отодвигает
таймер, а тишина (или событие `pause` посреди фразы) запускает повторные
`play()` — минуту для уже начавшейся фразы, десять секунд для не начавшейся.
Не вышло — обещание всё равно закрывается, чтобы очередь пошла дальше.
Плюс `navigator.audioSession.type = "playback"`: с ним система чаще приглушает
нашу озвучку, а не отнимает звук целиком.

## Сохранённая позиция карточки — только в layout-эффекте

Восстанавливать что-либо из `localStorage` прямо в инициализаторе `useState`
нельзя: на сервере хранилища нет, там всегда выходит первая карточка, клиент
рисует сохранённую — и React валит гидрацию («Hydration failed»), выбрасывая и
перерисовывая поддерево. Обычный `useEffect` тоже не годится: он отрабатывает
после кадра, и прыжок «карточка 1 → сохранённая» видно глазом. Правильное
место — `useLayoutEffect` (в SSR подменяется на `useEffect`, иначе React
предупреждает): после гидрации, но до отрисовки. Так сделано в
`components/flashcard-player.tsx`.

## Озвучка сказок: голос и подача named, а не числами

`scripts/generate-audio-skazki.mjs` — отдельный скрипт, не `--reading` у
`generate-audio-voicer.mjs`. Разница не косметическая: в читалке каждое
предложение это файл с именем-хэшем (тап по слову → звук), а сказку слушают
целиком, лёжа в кровати, поэтому здесь **один непрерывный трек на часть**.
Абзацы уходят чанками и склеиваются ffmpeg — заодно получаются паузы.

В озвучку входят мораль, крючок и «Спокойной ночи, Уля»: на сайте эти куски
рисует вёрстка, и без них запись обрывалась бы на полуслове.

Голоса и подача названы, чтобы к удачному варианту возвращались командой, а не
вспоминали числа:

```bash
npm run skazki:audio:voices                                  # каталог и пресеты
npm run skazki:audio -- --voice=sarah --preset=soft --parts=1,2
npm run skazki:audio:sample -- --voices=matilda --preset=soft
```

Пресеты: `plain` (0.85/0.75), `soft` (0.8/0.9), `lullaby` (0.75/0.95).
Отобранный вариант на 31.08.2026 — **ab9 + soft**, он же по умолчанию.

**Голоса на Voicer пропадают.** Список голосов API не отдаёт вовсе, id уходит в
ElevenLabs как есть, а клон может исчезнуть на их стороне — тогда задачи с ним
падают целиком. В каталоге скрипта помечены галочкой те, что реально отвечали
на нашем аккаунте; остальные сначала слушать пробой, а не гнать на них книгу.

**Квота списывается при создании задачи, а не по факту озвучки.** Поэтому id
каждой задачи сразу пишется в `data/audio-tasks.jsonl`. Вся книга стоит 171 346
символов — это больше, чем остаток пакета, так что озвучивать имеет смысл
частями и следить за `remaining_characters`.
